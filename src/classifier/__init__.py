"""GRU-based binary classifier for Notebook 6.

Consumes the FFT-filtered dataset produced by Notebook 5
(``models/train_sequences_fft.npy`` and ``models/train_labels.npy``) and
performs classification only: no FFT, no TimeGAN, no label generation
happens here.
"""
